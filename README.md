# Amzonsqlproject6
This SQL program creates a **school system database** with a **students table** storing student IDs, names, classes, and marks. It lists all students and uses **ROW_NUMBER()**, **RANK()**, and **DENSE_RANK()** functions to rank students by marks within each class. Finally, it deletes the database.
